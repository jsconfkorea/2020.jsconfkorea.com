import useRedirect from '../../hooks/useRedirect'
import { OUT_LINKS } from '../../utils/constants'

const CFP_FORM_EN = () => {
  useRedirect(OUT_LINKS.CFP_FORM.EN)
  return <></>
}

export default CFP_FORM_EN
