import useRedirect from '../hooks/useRedirect'
import { OUT_LINKS } from '../utils/constants'

const Schedule = () => {
  useRedirect(OUT_LINKS.HOPIN_SCHEDULE)
  return <></>
}

export default Schedule
